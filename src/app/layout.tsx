import type { Metadata } from "next";
import { Geist, Geist_Mono } from "next/font/google";
import "./globals.css";

const geistSans = Geist({
  variable: "--font-geist-sans",
  subsets: ["latin"],
  display: "swap",
});

const geistMono = Geist_Mono({
  variable: "--font-geist-mono",
  subsets: ["latin"],
  display: "swap",
});

export const metadata: Metadata = {
  title: "Schedy - Smart Scheduling for Teams and Individuals",
  description:
    "Streamline your calendar, boost productivity, and never miss an important meeting again with our intelligent scheduling platform.",
  keywords: [
    "scheduling",
    "calendar",
    "productivity",
    "meetings",
    "booking",
    "time management",
  ],
  authors: [{ name: "Schedy Team" }],
  creator: "Schedy Inc.",
};

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return (
    <html lang="en" className="scroll-smooth">
      <body
        className={`${geistSans.variable} ${geistMono.variable} antialiased font-sans`}
      >
        {children}
      </body>
    </html>
  );
}
